syntax = "proto3";

package media;

service MediaUpload {
  rpc UploadVideo (stream VideoChunk) returns (UploadStatus);
}

message VideoChunk {
  string filename = 1;
  bytes data = 2;
}

message UploadStatus {
  string message = 1;
  bool success = 2;
}
