#include "linked_list.h"


int main() {
    node linked_list = {{SENTINEL_VALUE, NULL}};

    if(linked_list.value == SENTINEL_VALUE) {
        set_node(linked_list, value);
    } else if (linked_list.pointer == NULL) {
        
    } else


    return 0;
}